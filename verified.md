# v0.2.0 验证记录

本记录说明仓库的 Skill 结构、路由边界和制作计划是否经过验证；不把任何平台能力、价格、模型输出或商业效果视为恒久事实。

## 本版范围

- 新增第 12 个总编排器：[`build-ai-video-fast`](./build-ai-video-fast/SKILL.md)。
- 完整项目入口：5–30 秒的 AI 广告、剧情短片、产品片或社交视频，交付单一 Markdown `AI Video Build Pack`。
- 保留 11 个原子 Skill 的单点边界：静态提示词、参考图反推、局部编辑、输入职责、模型档位、锚点、动态导演、分镜、端点与序列装配。

## 结构验证

- 官方 `quick_validate.py` 在 UTF-8 模式下验证 **12 / 12** 个 Skill 通过。
- 每个 Skill 的 frontmatter 仅含 `name` 与 `description`；名称、目录与 `test-prompts.json` 的 `skill` 字段一致。
- `build-ai-video-fast` 的 `agents/openai.yaml` 含有效的显示名、描述、默认调用提示和隐式调用策略。
- Build Pack 模板含固定 **11** 个章节；仓库共有 **78** 条 JSON 用例（原 66 + 新 12）。

## 回归与盲测

| 范围 | 方法 | 结果 |
| --- | --- | --- |
| 11 个原子 Skill | 三组独立只读回归复核，每个用例对照当前 Skill 的执行和边界规则。 | 66 / 66 通过 |
| 总编排器路由 | 12 条公开路由、负例、混合素材、缺失信息与超范围用例。 | 12 / 12 通过；见 [`build-ai-video-fast/test-results.md`](./build-ai-video-fast/test-results.md) |
| 端到端工作流 | 六条全新上下文独立盲测。 | 6 / 6 通过；见 [`validation/e2e-blind-test-results.md`](./validation/e2e-blind-test-results.md) |

## 已验证的边界

- 完整项目优先路由到 `build-ai-video-fast`；只优化静态图片、只反推单图、只做局部编辑、只比模型档位或只写单镜头时，仍进入相应原子 Skill。
- 混合素材仅选择一个主路线：动作/运镜优先于端点，端点优先于身份/产品一致性，一致性优先于纯文字发散。
- 时长低于 5 秒或超过 30 秒会明确标记为标准范围外，并提出端点微型方案、压缩或拆分路径；不会悄然篡改用户规格。
- 所有测试都只验证规划能力；不调用付费服务，不生成或导出成片，也不替用户确认授权。

## 已知限制

自然语言 Skill 没有可执行的模型路由器，因此测试是对 Skill 指令与独立代理输出的行为验证，而非某个部署环境的自动分发日志。实际成片仍需要按选定工具、素材权利、品牌规范和人工审片完成验收。